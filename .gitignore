node_modules
.wrangler
dist
.context
.dev.vars
.vars.dev
.DS_Store

# Per-run Impeccable critique / live snapshots — ephemeral, machine-local.
# PRODUCT.md and DESIGN.md (the canonical context files) are committed.
.impeccable/

# Test artifacts (Playwright, Vitest coverage, Lighthouse CI)
playwright-report/
test-results/
.lighthouseci/
coverage/
out-tsc/
# Vitest JSON reporter output from the CI integration job (AECI-90).
integration-results.json

# Wrangler-generated Worker type definitions (~15k lines each).
# Regenerated by each app's `cf-typegen` script and the root `postinstall`
# hook, so they exist locally for tsc/wrangler but never bloat the repo or
# pollute codebase search. Do NOT commit these.
**/worker-configuration.d.ts
