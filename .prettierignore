node_modules
dist
.angular
.wrangler
.agents
.claude
coverage
playwright-report
test-results
**/*.min.js
**/generated/**
pnpm-lock.yaml
*.md
public

# Frozen Phase-1 validation probes — archived outside the workspace (AECI-118).
# Not linted/formatted; kept only as line-anchor references for the docs.
spikes/

# Supabase CLI runtime state — gitignored, not for prettier
supabase/.temp/
supabase/.branches/

# drizzle-kit output — generated migrations + meta (re-dirtied on every db:generate).
# The .sql has no Prettier parser anyway; the meta/*.json must stay in drizzle's format.
apps/api/migrations/
